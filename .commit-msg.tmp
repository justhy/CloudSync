fix(web): 侧栏版本号不再重复 v 前缀

版本号来自 git tag（git describe --tags 会把 tag 原样带出，如 v0.0.1），
而 app-shell 无条件再拼一个 v，界面就显示成 vv0.0.1。

改为「先剥掉可能存在的 v 再加」，带 v / 不带 v 的版本串都正确：
  v0.0.1 -> v0.0.1
  0.0.1  -> v0.0.1

- web/src/components/app-shell.tsx: v${version} -> v${version.replace(/^v/i,"")}
- 重新构建前端产物并提交：index-J3_H5NPc.js -> index-C8fhmm3a.js，
  internal/web/static/index.html 同步引用新 hash（embed 进二进制）。

验证：tsc --noEmit 通过、vite build 通过；gofmt/vet/test 全绿；
重建 bin/cloudsync.exe 后确认新 JS 与修复逻辑确已 embed、旧 asset 不再存在。
